#this can be a comma separated list of servers, when we have 2 or more config servers.
spring.cloud.config.uri=http://localhost:8091
#Ensures app fails at startup if config server is inaccessible and not at runtime when accessing a property.
spring.cloud.config.failFast=true